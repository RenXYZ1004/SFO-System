The shirt size chart shown inside the "Shirt size" question on the
registration screen.

  shirt-size.png

That exact name and extension — the page asks for this one file, nothing
else. If the chart arrives as a jpg, convert it or change the reference in
the tpl-shirt-guide template in public/index.html.

The chart is shown whole, never cropped, with a "Full size" link so the
measurements can actually be read on a phone. A landscape image around
1200x800 works well. Keep it under about 300 KB.

With no file here the tile shows an "Image coming soon" placeholder and
hides the "Full size" link, so the question still works — runners simply
pick from the size chips as before.
