The two documents linked from "Before you register" on the front page:

  waiver-of-liability.pdf
  health-declaration.pdf

Both filenames are referenced from public/index.html, so replace these files
in place rather than renaming them. The placeholders currently sitting here
are NOT the official documents — swap in the signed versions from the SFO /
Alumni Department before the registration page goes live.
